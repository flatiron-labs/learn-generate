require "flatiron-lab-generator/version.rb"

module FlatironLabGenerator
  class TemplateMaker
    #selects and reads from the template directory that stores all templates
      # idea 1:
      # template.rb has the directory/file names as a nested hash. It iterates through and creates files and folders

    #calls git init

    #creates a .gitignore file

    #creates an .rspec file

    #ARGV[0] is template type
    #ARGV[1] is lab name
  end
end
